import type { BaseLayoutProps } from 'fumadocs-ui/layouts/shared';

// Shared config between the docs layout and any future top-level chrome
// (a landing page, etc.) — see fumadocs' own docs for the full option set.
export const baseOptions: BaseLayoutProps = {
  nav: {
    title: 'OSSPlay Docs',
  },
};
